# yaml-language-server: $schema=../../schemas/yaml/services_schema.yaml
# Describes the format for available services for topology.
#
# No service actions are exposed yet. Phase 8 introduces set/clear area
# annotations, add/remove/update connections, set occupancy extent,
# complete a floor level, and purge projected labels (see
# `docs/development/PLAN.md` §6 Phase 8).
