{
  "config": {
    "step": {
      "user": {
        "title": "Set up Topology",
        "description": "Topology annotates the Home Assistant area and floor registries with type, environment, trust, adjacency, and connections. There is no per-instance configuration — this creates the single hub entry."
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance of Topology can be configured."
    }
  }
}
